[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install -g pnpm@10.33.0", "pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm --filter @bin/web build"]

[start]
cmd = "pnpm --filter @bin/web start"
