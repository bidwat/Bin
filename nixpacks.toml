[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npx -y pnpm@10.33.0 install --frozen-lockfile"]

[phases.build]
cmds = ["npx -y pnpm@10.33.0 --filter @bin/web build"]

[start]
cmd = "npx -y pnpm@10.33.0 --filter @bin/web start"
