{
  "name": "@bin/mobile",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "node --watch src/index.ts",
    "dev:android": "node src/index.ts",
    "lint": "eslint src --ext .ts",
    "type-check": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@bin/shared": "workspace:*",
    "@bin/supabase": "workspace:*"
  }
}
